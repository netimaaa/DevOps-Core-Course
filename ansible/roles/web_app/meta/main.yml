---
# Role dependencies
# This ensures Docker is installed before deploying the web application

dependencies:
  - role: docker
    # Docker role must be executed first to ensure:
    # - Docker engine is installed
    # - Docker service is running
    # - User has docker group permissions
    # - Python docker module is available for Ansible