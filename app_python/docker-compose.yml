version: '3.8'

services:
  devops-info-service:
    build:
      context: .
    container_name: devops-info-service
    ports:
      - "8000:8000"
    environment:
      HOST: 0.0.0.0
      PORT: 8000
      DEBUG: "false"
      VISITS_FILE: /data/visits
    volumes:
      - ./data:/data
